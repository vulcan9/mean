// const path = require('path');

module.exports = [
    /*
    {
        // 호출할 가상 URL
        url: '/dev',
        path: 'ENV.web.root 위치에 대한 상대 경로',
        config: {
            // dotfiles: 'deny',
            // extensions : ['js']
            'index': ['index.html']
        }
    }
    */

    // http://localhost:4000/web/dev/
    {url: '/', path: './client'}
];
