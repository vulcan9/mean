// const path = require('path');

module.exports = [
    /*
    {
        // 호출할 가상 URL
        url: '/dev',
        path: 'global.ROOT.web 에서 상대 경로',
        config: {
            // dotfiles: 'deny',
            // extensions : ['js']
            'index': ['index.html']
        }
    }
    */

    // http://localhost:4000/web/dev/
    {url: '/', path: './client'}
];
